<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nguyenphuc.tracnghiem">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".MusicService"
            android:enabled="true" />

        <activity android:name=".MainActivity" />
        <activity android:name=".Slide.ScreenSlideActivity" />
        <activity android:name=".BoDe.BoDeActivity" />
        <activity
            android:name=".FlashScreenActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Slide.KetQuaActivity" />
        <activity android:name=".CaiDat.CaiDatActivity" />
        <activity android:name=".OnTap.OnTapActivity"></activity>
    </application>

</manifest>